#include<iostream>

struct Node {
    string key;
    string value;
    vector<Node*> next;
}

bool hasNext() {

}

Token getNext() {
        
}


void serializeJson () {

        Node *n = new Node (key,value);
        if (hasNext() ) {
            node->next.push_back (getNext());
        }
}

int main () {
    
}

